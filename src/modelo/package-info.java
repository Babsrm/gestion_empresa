/**
 * Paquete con todas las clases necesarias para la ejecución correcta de las funciones en cuanto a la obtención (getters) y colocación (setters) de los objetos.
 * 
 * @author Barbara Ruiz.
 */
package modelo;